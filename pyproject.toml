[project]
name = "game-server-manager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "docker",
    "fastapi",
    "semantic-version",
    "uvicorn[standard]",
    "python-dotenv",
]
